os: linux

language: java
services:
  - docker
cache: bundler
dist: focal

install: skip

jobs:
  include:
    - name: failing
      script: true

after_success:
  - echo done
  - echo $TRAVIS_TEST_RESULT
  - echo $SSH_KEY_FOR_SIGNING
after_failure:
  - echo failed
  - echo $TRAVIS_TEST_RESULT
